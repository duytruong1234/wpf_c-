<Window x:Class="QuanLyKhoNguyenLieuPizza.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Spinning animation for the pizza icon -->
        <Storyboard x:Key="SpinAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="PizzaRotate"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360" Duration="0:0:3"/>
        </Storyboard>

        <!-- Fade-in for the whole window -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Fade-out for closing -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Pulsing dots animation -->
        <Storyboard x:Key="DotPulse" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="Dot1"
                             Storyboard.TargetProperty="Opacity"
                             From="0.3" To="1" Duration="0:0:0.5"
                             AutoReverse="True" BeginTime="0:0:0"/>
            <DoubleAnimation Storyboard.TargetName="Dot2"
                             Storyboard.TargetProperty="Opacity"
                             From="0.3" To="1" Duration="0:0:0.5"
                             AutoReverse="True" BeginTime="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="Dot3"
                             Storyboard.TargetProperty="Opacity"
                             From="0.3" To="1" Duration="0:0:0.5"
                             AutoReverse="True" BeginTime="0:0:0.4"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
            <BeginStoryboard Storyboard="{StaticResource SpinAnimation}"/>
            <BeginStoryboard Storyboard="{StaticResource DotPulse}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="24" Width="380" Height="320">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFFFFF" Offset="0"/>
                <GradientStop Color="#FFF7ED" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="50" ShadowDepth="10"
                              Direction="270" Color="#1E293B" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <!-- Top accent bar -->
            <Border CornerRadius="24,24,0,0" Height="4" VerticalAlignment="Top">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#E85D04" Offset="0"/>
                        <GradientStop Color="#F48C06" Offset="0.5"/>
                        <GradientStop Color="#FFBA08" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Pizza icon with spin -->
                <Grid HorizontalAlignment="Center" Width="80" Height="80" Margin="0,0,0,20">
                    <!-- Glow ring -->
                    <Ellipse Width="80" Height="80" StrokeThickness="2" Opacity="0.3">
                        <Ellipse.Stroke>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#E85D04" Offset="0"/>
                                <GradientStop Color="#FFBA08" Offset="1"/>
                            </LinearGradientBrush>
                        </Ellipse.Stroke>
                    </Ellipse>
                    <!-- Inner pizza circle -->
                    <Ellipse Width="64" Height="64">
                        <Ellipse.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#E85D04" Offset="0"/>
                                <GradientStop Color="#F48C06" Offset="1"/>
                            </LinearGradientBrush>
                        </Ellipse.Fill>
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="PizzaRotate" CenterX="32" CenterY="32"/>
                        </Ellipse.RenderTransform>
                    </Ellipse>
                    <!-- Pizza slice icon -->
                    <Path Data="M12,2 C6.48,2 2,6.48 2,12 L12,12 Z M14,2.05 C18.73,2.56 22.44,6.27 22.95,11 L14,11 Z"
                          Fill="White" Stretch="Uniform"
                          Width="30" Height="30"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                </Grid>

                <!-- App title -->
                <TextBlock Text="Pizza Warehouse"
                           FontSize="22" FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,4">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#E85D04" Offset="0"/>
                            <GradientStop Color="#DC2F02" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                </TextBlock>

                <TextBlock Text="Quản Lý Kho Nguyên Liệu"
                           FontSize="13" Foreground="#64748B"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,28"/>

                <!-- Loading dots -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Ellipse x:Name="Dot1" Width="8" Height="8" Fill="#E85D04" Margin="0,0,6,0"/>
                    <Ellipse x:Name="Dot2" Width="8" Height="8" Fill="#F48C06" Margin="0,0,6,0"/>
                    <Ellipse x:Name="Dot3" Width="8" Height="8" Fill="#FFBA08"/>
                </StackPanel>

                <TextBlock Text="Đang tải..."
                           FontSize="12" Foreground="#94A3B8"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

