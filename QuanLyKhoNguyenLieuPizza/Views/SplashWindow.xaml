<Window x:Class="QuanLyKhoNguyenLieuPizza.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="ShimmerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="ShimmerTranslate"
                             Storyboard.TargetProperty="X"
                             From="-200" To="400" Duration="0:0:1.8">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
            <BeginStoryboard Storyboard="{StaticResource ShimmerAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border Width="400" Height="200" ClipToBounds="False" Background="Transparent">
        <Grid>
            <!-- Nội dung Chính -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                
                <!-- Logo & Subtitle -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="PizzaInn"
                               FontSize="34" FontWeight="Bold" Foreground="#F9FAFB"
                               FontFamily="Segoe UI Variable, Segoe UI, Arial"
                               VerticalAlignment="Center" Margin="0,0,14,0">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Color="Black" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    
                    <TextBlock Text="W A R E H O U S E"
                               FontSize="13" FontWeight="Bold" Foreground="#F59E0B"
                               FontFamily="Segoe UI Variable, Segoe UI, Arial"
                               VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Color="Black" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <!-- Progress bar -->
                <Grid Width="200" Height="4" HorizontalAlignment="Center" ClipToBounds="True" Margin="0,0,0,24">
                    <Grid.Effect>
                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="Black" Opacity="0.4"/>
                    </Grid.Effect>
                    <!-- Track -->
                    <Border CornerRadius="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="0.5"/>

                    <!-- Moving shimmer -->
                    <Border CornerRadius="2" Width="80" HorizontalAlignment="Left">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#E85D04" Offset="0.3"/>
                                <GradientStop Color="#F48C06" Offset="0.5"/>
                                <GradientStop Color="#FFBA08" Offset="0.7"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.RenderTransform>
                            <TranslateTransform x:Name="ShimmerTranslate" X="-200"/>
                        </Border.RenderTransform>
                    </Border>
                </Grid>

                <!-- Loading text -->
                <TextBlock Text="Đang khởi động..."
                           FontSize="12" Foreground="#E2E8F0"
                           FontFamily="Segoe UI Variable, Segoe UI, Arial"
                           HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="Black" Opacity="0.6"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>

            <!-- Version info at bottom right -->
            <TextBlock Text="v1.0.0"
                       FontSize="11" Foreground="#94A3B8"
                       HorizontalAlignment="Right" VerticalAlignment="Bottom"
                       Margin="0,0,16,12"
                       FontFamily="Segoe UI Variable, Segoe UI, Arial">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Color="Black" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
    </Border>
</Window>
